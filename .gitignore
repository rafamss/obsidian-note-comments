# Dependencies
node_modules/

# Build output (generated by esbuild; attached to releases, not versioned)
main.js
*.js.map

# Plugin runtime data (per vault)
data.json

# Local machine config (Claude Code / local permissions)
.claude/settings.local.json

# Operating system
.DS_Store
